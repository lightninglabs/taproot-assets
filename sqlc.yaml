version: "2"
sql:
  - engine: "postgresql"
    schema: "tarodb/sqlite/migrations"
    queries: "tarodb/sqlite/queries"
    gen:
      go:
        out: tarodb/sqlite
        package: sqlite
        emit_interface: true
